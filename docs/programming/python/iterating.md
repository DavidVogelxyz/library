Iterating in Python
===================

To iterate effectively in Python, use the built-in Python `for` loop:

```python
string = "hello"

for c in string:
    print(f"{c}")
```

Prints:

```
h
e
l
l
o
```
